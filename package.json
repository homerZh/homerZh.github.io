{
  "name": "homer-tools",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/admin.js src/calories.js src/blood-pressure.js --bundle --minify --outdir=assets --platform=browser",
    "test": "node --test tests/calorie-math.test.mjs tests/blood-pressure-time.test.mjs",
    "preview": "node scripts/preview.cjs"
  },
  "dependencies": {
    "@cloudbase/js-sdk": "3.10.0",
    "esbuild": "0.28.2"
  }
}

// Copy to cloudbase.config.js after creating your CloudBase environment.
// Environment ID and region are public identifiers, not credentials.
// Never add passwords, SecretId, SecretKey, or server API keys here.
// This template is not loaded by the homepage yet.
window.CLOUDBASE_CONFIG = Object.freeze({
  env: "", // Environment ID from the CloudBase console
  region: "" // Exact environment region, e.g. ap-shanghai
});
